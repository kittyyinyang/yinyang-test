# Evidence 时间戳修复 · 进程交接文档

> 日期：2026-08-31 ｜ 背景：v2.0 上线后用户发现——知识卡片「语料佐证」的按钮跳转不落到视频中说这句话的节点，而是从视频开头开始。本文档为**修复进程交接**：已完成什么、根因是什么、下一步精确怎么做。
> 工作目录：`D:\Workbuddy\yinyang-test\`；语料：`D:\Workbuddy\2026-08-26-20-11-15\downloads\语料全量版-最新.md`

---

## 一、问题与根因（已确诊）

**现象**：点佐证「▶」播放钮 → 打开的视频从头播放，不在卡文对应位置。

**根因排查结论（两类）**：
1. **真错配（约 13+ 条，已捕获）**：卡片 ts/link 与原话实际位置不符。例：`q-B714-01` 卡记 `B714@01:17`，实测原话「情是作为一个人的需求」在 **B714@01:22**（语料 89518 行）；`B714@04:08` 实际在 **@04:03(243s)**；`q-y1-17-01` 的 `B605@03:51` 实际在 **@03:54(234s)**。多为内容 Agent 计时差 3–5 秒或引用行偏移。
2. **卡文润色类（约 113 条 NOTFOUND 的主体）**：卡文是**润色改写**（如 B680「女阴，就是女性的阴性能量——她们的内在世界感情最重要」在语料中**不存在逐字对应**——内容 Agent 按知识库语序重写；B44 加了「……」省略号；B314「四个陌生人都可以进来逛一下」实际在 @02:47 卡记 @02:43=行近似）。**这类多数跳转大体正确（差 1-5 秒）或无法机器判定，需人工归类**：要么修 ts 对齐语料行，要么改卡文为逐字原话。

⚠️ **重要**：`data_qa.js` 已被内容 Agent 重构过（注释注明「2026-08-31: 第一批次切片卡 q-c-* 共 52 条整体移除，core 体系为唯一问答数据源」；字段名是 `answer` 非 `evidence`、双引号 JSON、末尾有 `RESULT_GUIDE_MAP` 导致简单 JSON 解析在 line 2396 报 Extra data——需只取数组部分）。**qa_clips_new.js 的 52 条已不在站内使用**（deprecated/移除），修复优先级：qa_core1-4 > data_qa.js。

## 二、已完成（可直接复用）

| 产物 | 位置 | 说明 |
|---|---|---|
| 校验器 v2（**现用版**） | `scripts\verify_evidence_v2.py` | 语料全量解析（664 视频、`### Bxxx` section + bv 行 + `[mm:ss]` 行）→ 卡片全量解析（单/双引号兼容，answer/evidence 两字段名）→ 滑窗行级匹配（首句前 18 规范化字符，跨 1-3 行）→ 输出 ok/mismatch/notfound 三态 |
| 校验基线（2026-08-31 跑出） | `scripts\_ev_issues.json` | 明细：一致 93 ｜ 错配 13 ｜ 未定位/过短 113 |
| 上一版校验器 | `scripts\verify_evidence.py` | 弃用（字段顺序假设错误） |
| 匹配算法要点 | verify_evidence_v2.py 内 `norm()` + `find_in()` | 规范化=去空白标点+小写；跨行拼接最多 3 行 |

**已抽查证实（人工证据，可直接引用）**：
- B39@02:36「人在遇到同一个场景」逐字存在（语料 22702 行）→ 卡正确的样本，此前 NOTFOUND 是匹配器太严；
- B714 真错配：卡 01:17 → 实际 01:22（89518 行）；
- B314：卡 02:43 → 实际 02:47（58493 行）。

## 三、下一步执行清单（按序）

### Step 1：修真错配（13 条，机器可全自动）
`_ev_issues.json` 中 `实际:REAL:Bxxx@sec` 的条目，按 `fix` 值改卡片源文件对应 evidence 的 `ts/bv/link`（link 的 `?t=` 秒数同步）。注意同卡多条 evidence 逐条改；改完重跑校验器确认这些条目转 ok。

### Step 2：归类 NOTFOUND 113 条（半自动，工作量主体）
每条人工看卡文与语料对应视频：
- **A 类（跳转近似正确，卡文润色）**：在语料中找到卡文「首句的最近似行」，把 ts 对齐该行——卡文不动，只修跳转精度；
- **B 类（卡文无法逐字对应）**：改卡文为该时间窗的逐字原话（保持原义，ASR 音近还原规则见 `docs\知识切片_含金量升级规范_v2.md`）；
- **C 类（语料确实没有，Agent 编造嫌疑）**：标注进报告交用户裁决（删或换佐证）。
可先写辅助脚本：对每条 NOTFOUND 在声称视频内做「最长公共子串 ≥10 字符」的行定位，输出建议 ts，人工只做确认。

### Step 3：修 data_qa.js 解析失败
校验器对 data_qa.js 报 `Extra data: line 2396`——数组后有 `RESULT_GUIDE_MAP` 对象。修法：`parse_js()` 里取**第一个 `[` 到与之配对的 `]`**（括号计数），不要用 `rindex(']')`。修好后把该文件纳入 Step 1/2 范围（它当前已瘦身到 70 条核心转换卡）。

### Step 4：回归 + 部署
1. 重跑 `python scripts/verify_evidence_v2.py` 直至：错配 0、NOTFOUND 仅剩人工标注的 C 类；
2. 本地 reload 抽查 5 条（`http://127.0.0.1:8018/index.html#qa` 强刷）；
3. `git add -A && git commit -m "fix: evidence 时间戳全量校正" && git push origin HEAD:main`（⚠️ GitHub 443 间歇断连，失败等 30-60s 重试，实测有窗口期；凭据 manager 已通）；
4. 线上验证：打开正式站任一卡点播放钮确认落点。

## 四、关联资产
- 卡片规范：`docs\知识切片_含金量升级规范_v2.md`（evidence「原文不改义、ASR 最小还原」纪律）
- 生产 skill：`C:\Users\47173\.workbuddy\skills\yinyang-card-factory\SKILL.md`
- 语料编号规则：B+数字 / N 系列（N1-5 无 bv 不进搜索）；同一 id 可能跨篇章多 section（撞号源，B23 已知），定位必须「id+section 内匹配」不能只按 id 取第一个 section。

## 五、其他在途事项（勿忘）
- 首页微调半成品：`scripts\_home_fix.py` 第 2 处 assert 失败（「进入搜一搜」按钮后的 HTML 缩进与预期不符，需现场读实际 HTML 对齐锚点）；第 1 处已验证可命中。
- AGENTS.md 已更新至部署后状态（含本事项外的全部上下文）。
